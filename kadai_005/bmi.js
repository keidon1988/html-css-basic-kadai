// 体重（kg）と身長（m）を変数に代入
let weight = 68;     // 体重
let height = 1.7;    // 身長

// BMIの計算
let bmi = weight / (height * height);

// コンソールに出力
console.log("体重：" + weight + "kg");
console.log("身長：" + height + "m");
console.log("BMI：" + bmi);
